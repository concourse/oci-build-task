---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: concourse/builder-task
    tag: buildkit

inputs:
- name: builder-task
  path: .

outputs:
- name: image

caches:
- path: cache

run: {path: build}
