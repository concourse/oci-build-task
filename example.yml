---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: concourse/builder-task
    tag: buildkit

params:
  CONTEXT: builder-task

inputs:
- name: builder-task

outputs:
- name: image

caches:
- path: cache

run: {path: build}
